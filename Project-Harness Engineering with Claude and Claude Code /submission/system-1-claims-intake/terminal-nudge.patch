diff --git a/Build a Claims Intake Agent with a stop_reason-Driven Loop/exercises/03-dynamic-decomposition/solution/claims_intake/loop.py b/Build a Claims Intake Agent with a stop_reason-Driven Loop/exercises/03-dynamic-decomposition/solution/claims_intake/loop.py
index 65aebe9..8e565ce 100644
--- a/Build a Claims Intake Agent with a stop_reason-Driven Loop/exercises/03-dynamic-decomposition/solution/claims_intake/loop.py	
+++ b/Build a Claims Intake Agent with a stop_reason-Driven Loop/exercises/03-dynamic-decomposition/solution/claims_intake/loop.py	
@@ -29,6 +29,20 @@ ToolExecutor = Callable[[str, dict[str, Any]], str]
 """Takes (tool_name, tool_input) and returns the tool_result content as a string.
 Errors are returned as a serialized JSON string with is_error=true; never raised."""
 
+TerminationCheck = Callable[[], "str | None"]
+"""Asked once per `end_turn`, before the loop returns.
+
+Returns None when the caller is satisfied the task actually finished, or a
+corrective message to hand back to the model when it ended its turn without
+doing so. The loop stays domain-agnostic: it never inspects assistant text or
+tool names to decide what "finished" means — the caller owns that definition.
+"""
+
+DEFAULT_MAX_TERMINAL_NUDGES = 2
+"""How many times a premature `end_turn` may be corrected before the loop gives
+up and returns anyway. Not an iteration cap on the conversation — the Budget
+remains the only bound on that — just a bound on this one repair path."""
+
 
 class MessagesClient(Protocol):
     def create(self, **kwargs: Any) -> Any: ...
@@ -54,16 +68,25 @@ def run(
     budget: Budget,
     tracer: Tracer,
     max_tokens: int = 4096,
+    termination_check: "TerminationCheck | None" = None,
+    max_terminal_nudges: int = DEFAULT_MAX_TERMINAL_NUDGES,
 ) -> FinalState:
     """Run the agentic loop until the model returns stop_reason == "end_turn".
 
     The loop condition is `response.stop_reason == "tool_use"` — nothing else.
     There is no `while turn < N` cap. The Budget object is the safety mechanism.
+
+    When `termination_check` is supplied, an `end_turn` that the caller does not
+    consider terminal is corrected rather than accepted: the loop appends the
+    returned message and continues. This is still stop_reason-driven — `end_turn`
+    is what triggers the check — it just stops treating the model's decision to
+    stop as automatically correct.
     """
     working_messages = list(messages)
     turn = 0
     total_input = 0
     total_output = 0
+    nudges_used = 0
 
     while True:
         turn += 1
@@ -102,6 +125,21 @@ def run(
 
         if response.stop_reason == "end_turn":
             working_messages.append({"role": "assistant", "content": response.content})
+
+            correction = termination_check() if termination_check is not None else None
+            if correction is not None and nudges_used < max_terminal_nudges:
+                nudges_used += 1
+                working_messages.append({"role": "user", "content": correction})
+                tracer.write(
+                    {
+                        "turn": turn,
+                        "event": "terminal_nudge",
+                        "nudges_used": nudges_used,
+                        "reason": "end_turn without a terminal action",
+                    }
+                )
+                continue
+
             return FinalState(
                 messages=working_messages,
                 total_input_tokens=total_input,
diff --git a/Build a Claims Intake Agent with a stop_reason-Driven Loop/exercises/03-dynamic-decomposition/solution/claims_intake/run.py b/Build a Claims Intake Agent with a stop_reason-Driven Loop/exercises/03-dynamic-decomposition/solution/claims_intake/run.py
index 7999eb0..ba63700 100644
--- a/Build a Claims Intake Agent with a stop_reason-Driven Loop/exercises/03-dynamic-decomposition/solution/claims_intake/run.py	
+++ b/Build a Claims Intake Agent with a stop_reason-Driven Loop/exercises/03-dynamic-decomposition/solution/claims_intake/run.py	
@@ -63,6 +63,24 @@ def _run_one(
         max_input_tokens=max_input_tokens,
         max_wall_clock_s=max_wall_clock_s,
     )
+    def _terminal_check() -> str | None:
+        """A claim is finished only once a terminal tool has fired.
+
+        Lives here, not in loop.py: "finished" is a claims-domain fact, and the
+        loop stays generic. Returning a message rather than raising lets the
+        model recover in-conversation instead of losing the whole run.
+        """
+        if session.terminal_called:
+            return None
+        return (
+            "You ended your turn without taking a terminal action. Every claim must "
+            "end with exactly one call to route_to_adjuster or escalate_to_human. "
+            "If you still need information from the claimant, call request_clarification "
+            "— do not ask in plain text, because the claimant only ever sees that tool. "
+            "If the missing information cannot be obtained, call escalate_to_human now "
+            "with what you have."
+        )
+
     trace_path = run_dir / "traces" / f"{fixture['claim_id']}.jsonl"
     t0 = time.monotonic()
     state: FinalState | None = None
@@ -78,6 +96,7 @@ def _run_one(
                 tool_executor=executor,
                 budget=budget,
                 tracer=tracer,
+                termination_check=_terminal_check,
             )
         except (BudgetExceeded, UnexpectedStopReason) as exc:
             error = f"{type(exc).__name__}: {exc}"
